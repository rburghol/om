#!/bin/sh
# wrapper around php script
olddir=`pwd`
cd /opt/model/apache/batch_updates/
echo "php batchedit_subcomp_props.php $1 "$2" "$3" "$4" "$5" "$6" "$7""
php batchedit_subcomp_props.php $1 "$2" "$3" "$4" "$5" "$6" "$7"
cd $pwd
